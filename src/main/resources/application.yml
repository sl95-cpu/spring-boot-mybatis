spring:
  datasource:
    url: jdbc:mysql://localhost:3306/persons?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
  messages:
    basename: i18n/login
  mvc:
    date-format: yyyy-MM-dd
server:
  servlet:
    context-path: /sl
mybatis:
  type-aliases-package: com.sl.srpingboot001.pojo
  mapper-locations: classpath:mybatis/mapper/*.xml
